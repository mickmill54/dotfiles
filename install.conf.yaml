- defaults:
    link:
      create: true
      relink: true

- clean:
    - '~'
    - '~/.config'

- link:
    ~/.gitconfig:
      path: files/gitconfig

    # ~/.config/ files
    ~/.config/fontconfig/fonts.conf:
      path: files/config/fontconfig/fonts.conf
